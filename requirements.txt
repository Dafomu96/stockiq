# StockIQ — Python dependencies
# Pin minor versions for reproducibility; allow patch updates (~=).
# Split into sections: data, analysis, web, infra, dev.

# --- Data ---
yfinance~=0.2.40
pandas~=2.2.0
numpy~=1.26.0
requests~=2.31.0

# --- Analysis ---
pandas-ta~=0.3.14b
scipy~=1.13.0
statsmodels~=0.14.0

# --- Config ---
pydantic~=2.7.0
pydantic-settings~=2.3.0
python-dotenv~=1.0.0

# --- Structured logging ---
structlog~=24.2.0

# --- Web (Phase 1–2: Streamlit MVP) ---
streamlit~=1.36.0
plotly~=5.22.0

# --- Web (Phase 3: FastAPI backend) ---
fastapi~=0.111.0
uvicorn[standard]~=0.30.0
slowapi~=0.1.9        # rate limiting for FastAPI

# --- Dev / testing ---
pytest~=8.2.0
pytest-cov~=5.0.0
pytest-mock~=3.14.0
responses~=0.25.0     # mock HTTP responses in tests
httpx~=0.27.0         # async test client for FastAPI

# --- Code quality (enforced in CI) ---
ruff~=0.4.0
mypy~=1.10.0
black~=24.4.0
pre-commit~=3.7.0
